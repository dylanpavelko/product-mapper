<%= form_for(@capacity) do |f| %>
  <% if @capacity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@capacity.errors.count, "error") %> prohibited this capacity from being saved:</h2>

      <ul>
      <% @capacity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :team_id %><br>
    <%= f.text_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :milestone_id %><br>
    <%= f.text_field :milestone_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
