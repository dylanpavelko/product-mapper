updated the status of <% @node = Node.find(activity.parameters[:node_id]) %><%= link_to @node.name, @node %>
	<div>
		Updated  <%= activity.trackable.phaseType.name %> to  <%= activity.trackable.get_progress_status activity.parameters[:progress_status] %>
	</div>