<%= form_for(@agenda_item) do |f| %>
  <% if @agenda_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agenda_item.errors.count, "error") %> prohibited this agenda_item from being saved:</h2>

      <ul>
      <% @agenda_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :meeting_id %><br>
    <% if @meeting == nil %>
      <%= f.select :meeting_id, options_from_collection_for_select(Meeting.all.sort_by {|t| t.display}, :id, :display), {:include_blank => true}, {:class => "form-control"} %>
    <% else %>
      <%= f.select :meeting_id, options_from_collection_for_select(Meeting.all.sort_by {|t| t.display}, :id, :display), {:include_blank => true}, {:class => "form-control"} %>
    <% end %>
  </div>
  <div class="field">
    <%= f.label :order %><br>
    <%= f.number_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
