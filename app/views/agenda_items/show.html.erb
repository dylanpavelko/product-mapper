<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @agenda_item.name %>
</p>

<p>
  <strong>Meeting:</strong>
  <%= link_to @agenda_item.meeting.display, @agenda_item.meeting %>
</p>

<p>
  <strong>Order:</strong>
  <%= @agenda_item.order %>
</p>

<p>
  <strong>Resulting Action Items:</strong>
  <ul>
    <% @action_items.each do |action| %>
      <li><%= action.name %></li>
    <% end %>
    <%= link_to "Add Action Item", new_action_item_path %>
  </ul>
</p>

<%= link_to 'Edit', edit_agenda_item_path(@agenda_item) %> |
<%= link_to 'Back', agenda_items_path %>
