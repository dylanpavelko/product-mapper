<%= form_for(@asana_task) do |f| %>
  <% if @asana_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@asana_task.errors.count, "error") %> prohibited this asana_task from being saved:</h2>

      <ul>
      <% @asana_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    URL<br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :asana_id %><br>
    <%= f.text_field :asana_id %>
  </div>
  <div class="field">
    <%= f.label :asana_workspace_id %><br>
    <% if @asana_task.asana_workspace_id != nil %>
    <%= f.select :asana_workspace_id, options_from_collection_for_select(AsanaWorkspace.all, :id, :workspace, @asana_task.asana_workspace_id), :include_blank => true %>
  <% else %>
    <%= f.select :asana_workspace_id, options_from_collection_for_select(AsanaWorkspace.all, :id, :workspace), :include_blank => true %>
  <% end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
