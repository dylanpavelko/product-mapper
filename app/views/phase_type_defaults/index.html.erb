<h1>Listing phase_type_defaults</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @phase_type_defaults.each do |phase_type_default| %>
      <tr>
        <td><%= link_to 'Show', phase_type_default %></td>
        <td><%= link_to 'Edit', edit_phase_type_default_path(phase_type_default) %></td>
        <td><%= link_to 'Destroy', phase_type_default, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Phase type default', new_phase_type_default_path %>
