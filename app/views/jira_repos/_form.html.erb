<%= form_for(@jira_repo) do |f| %>
  <% if @jira_repo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jira_repo.errors.count, "error") %> prohibited this jira_repo from being saved:</h2>

      <ul>
      <% @jira_repo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
