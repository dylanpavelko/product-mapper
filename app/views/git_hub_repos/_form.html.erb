<%= form_for(@git_hub_repo) do |f| %>
  <% if @git_hub_repo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@git_hub_repo.errors.count, "error") %> prohibited this git_hub_repo from being saved:</h2>

      <ul>
      <% @git_hub_repo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :repo %><br>
    <%= f.text_field :repo %>
  </div>
  <div class="field">
    <%= f.label :node_id %><br>
    <%= f.select :node_id, options_from_collection_for_select(Node.all, :id, :name), :include_blank => true %>
  </div>
  <div class="field">
    <%= f.hidden_field :user_id, :value => @current_user.id%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
