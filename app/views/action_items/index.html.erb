<h1>Listing action_items</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Complete</th>
      <th>Completed</th>
      <th>Due override</th>
      <th>Due by meeting</th>
      <th>Action from agenda item</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @action_items.each do |action_item| %>
      <tr>
        <td><%= action_item.name %></td>
        <td><%= action_item.description %></td>
        <td><%= action_item.complete %></td>
        <td><%= action_item.completed %></td>
        <td><%= action_item.due_override %></td>
        <td><%= action_item.due_by_meeting %></td>
        <td><%= action_item.action_from_agenda_item %></td>
        <td><%= link_to 'Show', action_item %></td>
        <td><%= link_to 'Edit', edit_action_item_path(action_item) %></td>
        <td><%= link_to 'Destroy', action_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Action item', new_action_item_path %>
